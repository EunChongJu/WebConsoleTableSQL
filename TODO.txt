이 프로젝트는 단순히 SQL을 웹에서도 동작하기 위한 프로젝트입니다.
클라이언트가 곧 서버, 즉 데이터베이스라는 삼위일체(?)에 맞게 개발하는 프로젝트입니다.
이 프로젝트는 바닐라 자바스크립트로 작동합니다. 또한 얼마든지 확장할 수 있습니다.

할 일 (완료하면 -를 V로 처리하시오)
- 먼저 웹에 표시될 구조를 마크업 하십시오.

- 자바스크립트에서 MVC 모델 또는 MVVC 구조로 설계하거나 설계상 편리한 구조로 설계하시오.
	(SQL 구문을 해석하는 프로그램과 SQL 함수, 그리고 SQL Data를 관리하는 클래스로 분리하면 좋을 것 같다.)
	[SQL 구문 해석 - parser.js, SQL 데이터 - data.js, SQL 함수 - function.js]
	
- 이 구조에서 각자 역할과 사용할 때 어떤 원리로 작동되는지 기술하시오.
	(SQL 구조에서 다음과 같이 설계하면 좋을 것이다)
	[SQL 구문 해석]  [SQL 함수]                     [SQL 데이터]
	SQL 구문 해석 -> 구문 해석에 따른 SQL 함수 호출 -> SQL 함수 처리에 따라 데이터 (추가, 변동, 삭제, 관리 등)
	- SQL 구문 해석:
		- SQL 명령어를 스페이스바 기준이나 특수문자 기준으로 나누고, 순서에 따라 어떤 값이 나오는지에 따라 처리한다.
		- 
	- SQL 함수:
		- SQL의 데이터 처리를 묶어서 함수로 만들어 처리한다.
		- 여러 개의 데이터를 갱신하거나 삭제, 추가, 수정을 할 수 있도록 한다.
		- 
	- SQL 데이터:
		- SQL의 데이터베이스에 저장되는 값을 데이터 형식에 맞춰 처리하고 저장한다.
		- 
- SQL에서의 DataBase의 저장 방식을 채택하시오.
	- 채택됨: key-value 방식

- 먼저 구문을 분석하여 어떤 타입의 구문인지 반환할 수 있도록 하시오.
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 데이터베이스와 SQL에서 함수를 실행하는 것을 처리하시오. (본 내용은 책을 참고하였음)
	
	- 데이터베이스와 SQL
	
	- 테이블에서 데이터 검색
		- Hello World 실행하기
			- SELECT * FROM table_name
			- SELECT 명령 구문
			- 예약어와 데이터베이스 객체명
			- Hello World를 실행한 결과 = 테이블
			- 값이 없는 데이터 = NULL

		- 테이블 구조 참조하기
			- DESC 명령
			- 자료형

		- 검색조건 지정
			- SELECT 구에서 열 지정하기
			- WHERE 구에서 행 지정하기
			- 문자열형의 상수
			- NULL 값 검색
			- 비교 연산자

		- 조건 조합하기
			- AND로 조합하기
			- OR로 조합하기
			- AND와 OR를 사용할 경우 주의할 점
			- NOT으로 조합

		- 패턴 매칭에 의한 검색
			- LIKE로 패턴 매칭하기
			- LIKE로 %를 검색하기
			- 문자열 상수`의 이스케이프!
	
	-정렬과 연산
		- 정렬 - ORDER BY
			- ORDER BY로 검색 결과 정렬하기
			- ORDER BY DESC로 내림차순으로 정렬하기
			- 대소 관계
			- ORDER BY는 테이블에 영향을 주지 않는다
		- 복수의 열을 지정해 정렬하기
			- 복수 열로 정렬 지정
			- 정렬방법 지정하기
			- NULL 값의 정렬 순서
		- 결과 행 제한하기 - LIMIT
			- 행수 제한
			- 오프셋 지정
		- 수치 연산
			- 사칙 연산
			- SELECT 구로 연산하기
			- 열의 별명
			- WHERE 구에서 연산하기
			- NULL 값의 연산
			- ORDER BY 구에서 연산하기
			- 함수
			- ROUND 함수
		- 문자열 연산
			- 문자열 결합
			- SUBSTRING 함수
			- TRIM 함수
			- CHARACTER_LENGTH 함수
		- 날짜 연산
			- SQL에서의 날짜
			- 날짜의 덧셈과 뺄셈
		- CASE 문으로 데이터 변환하기
			- CASE 문
			- 또 하나의 CASE 문
			- CASE를 사용할 경우 주의사항
		
	- 데이터의 추가, 삭제, 갱신
		- 행 추가하기 - INSERT
			- INSERT로 행 추가하기
			- 값을 저장할 열 지정하기
			- NOT NULL 제약
			- DEFAULT
		- 삭제하기 - DELETE
			- DELETE로 행 삭제하기
			- DELETE 명령 구
		- 데이터 갱신하기 - UPDATE
			- UPDATE로 데이터 갱신하기
			- UPDATE로 갱신할 경우 주의사항
			- 복수열 갱신
			- NULL로 갱신하기
		- 물리 삭제와 논리 삭제
			- 두 종류의 삭제 방법
			- 삭제방법 선택하기
	
	- 집계와 서브쿼리
		- 행 개수 구하기 - COUNT
			- COUNT로 행 개수 구하기
			- 집계함수와 NULL 값
			- DISTINCT로 중복 제거
			- 집계함수에서 DISTINCT
		- COUNT 이외의 집계함수
			- SUM으로 합계 구하기
			- AVG로 평균내기
			- MIN/MAX로 최솟값/최댓값 구하기
		- 그룹화 - GROUP BY
			- GROUP BY로 그룹화
			- HAVING 구로 조건 지정
			- 복수열의 그룹화
			- 결과값 정렬
		- 서브 쿼리
			- DELETE의 WHERE 구에서 서브쿼리 사용하기
			- 스칼라 값
			- SELECT 구에서 서브쿼리 사용하기
			- SET 구에서 서브쿼리 사용하기
			- FROM 구에서 서브쿼리 사용하기
			- INSERT 명령과 서브쿼리
		- 상관 서브쿼리
			- EXISTS
			- NOT EXISTS
			- 상관 서브쿼리
			- IN
	
	- 데이터베이스 객체 작성과 삭제
		- 데이터베이스 객체
			- 데이터베이스 객체
			- 스키마
		- 테이블 작성/삭제/변경
			- 테이블 작성
			- 테이블 삭제
			- 테이블 변경
			- ALTER TABLE로 테이블 관리
		- 제약
			- 테이블 작성시 제약 정의
			- 제약 추가
			- 제약 삭제
			- 기본키
		- 인덱스 구조
			- 인덱스
			- 검색에 사용하는 알고리즘
			- 일성
		- 인덱스 작성과 삭제
			- 인덱스 작성
			- 인덱스 삭제
			- EXPLAIN
			- 최적화
		- 뷰 작성과 삭제
			- 뷰
			- 뷰 작성과 삭제
			- 뷰의 약점
	
	- 복수의 테이블 다루기
		- 집합 연산
			- SQL과 집합
			- UNION으로 합집합 구하기
			- 교집합과 차집합
		- 테이블 결합
			- 곱집합과 교차결합
			- 내부결합
			- INTER JOIN으로 내부결합하기
			- 내부결합을 활용한 데이터 관리
			- 외부결합
		- 관계형 모델
			- 관계형 모델
			- 관계형 모델과 SQL
	
	- 데이터베이스 설계 (이건 사실상 필요 없음 : 나중에 가능하면 설계를 편하게 해주는거 만들든가)

- 
- 

(Aug 3, 2020 ~ ) Made by Eun Chong Ju, Noel